#include "main.h"

/**
 * _isupper - Entry point
 * a function to check for uppercase alpha
 * @c: an integer parameter
 * Return: On success uppercase
 */
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
