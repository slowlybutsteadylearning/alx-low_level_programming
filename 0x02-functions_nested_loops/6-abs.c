#include "main.h"
/**
 * _abs - Entry point
 * computes absolute value of an integer
 * @n: parameter passed
 * Return: absolute value of an integer
 */
int _abs(int n)
{
if (n < 0)
n = -n;
return (n);
}
