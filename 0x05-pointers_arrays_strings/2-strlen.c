#include "main"
#include <string.h>

/**
 * _strlen - Entry point
 * function that returns the length of a string
 * @s: string pointer
 * Return: length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}

